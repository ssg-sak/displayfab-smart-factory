# NOTES

이 파일은 예전에 적어 둔 Q&A 메모다.
Lab 자체의 목적은 여기 있는 문답 연습이 아니다.
DisplayFab Ops Lab은 OLED/LCD 데이터 흐름을 직접 돌려보려고 만든 축소 시뮬레이터다.

---

## 1. LOT이란 무엇인가?

**답변 포인트:** 함께 흐르는 작업 묶음. Panel 여러 장이 하나의 Recipe와 상태를 공유한다.

30~60초:

> LOT는 생산을 묶어 관리하는 단위입니다. 이 프로젝트에서는 Panel 여러 장이 하나의 LOT에 속하고, LOT마다 사용해야 하는 Recipe가 지정됩니다. 지금 그 LOT가 어느 설비에 있는지, WAIT/PROCESSING/HOLD/COMPLETE 중 어떤 상태인지를 LOT 단위로 봅니다. Panel은 그 안에서 이력을 남기는 개별 추적 단위입니다.

---

## 2. Recipe란 무엇인가?

**답변 포인트:** 설비가 그 공정에서 써야 하는 조건 세트. 이번 Lab에서는 온도/압력 범위.

30~60초:

> Recipe는 특정 공정을 수행할 때 설비가 사용해야 하는 조건 세트입니다. 실제 OLED Recipe는 훨씬 복잡하지만, 여기서는 학습용으로 기대 온도 범위와 압력 범위만 넣었습니다. LOT에 기대 Recipe가 있고, 설비가 다른 Recipe를 보고하면 mismatch로 처리합니다. 목적은 잘못된 공정 조건으로 생산이 진행되는 것을 시스템이 막는 개념을 이해하는 것입니다.

---

## 3. 왜 Panel마다 이력을 남기는가?

**답변 포인트:** 불량/이상 조건이 나왔을 때 그 기판이 어떤 설비·조건·시각을 거쳤는지 되짚기 위해.

30~60초:

> LOT 상태만 있으면 ‘그 묶음이 돌고 있다’는 것만 보입니다. 문제가 생겼을 때 필요한 질문은 특정 Panel이 LOAD, PROC, INSPECT를 언제, 어떤 Recipe로 거쳤는가입니다. 그래서 process_event를 Panel FK로 남기고 Traceability 화면에서 시간순으로 보게 했습니다. 특정 고객 규정 이야기가 아니라, 원인 범위를 좁히기 위한 소프트웨어 이력입니다.

---

## 4. 설비가 OFFLINE인지 어떻게 판단했는가?

**답변 포인트:** 값이 이상한지가 아니라, 데이터가 일정 시간 안 들어왔는지.

30~60초:

> 이벤트가 들어오면 equipment.last_seen_at을 갱신하고 Connection을 ONLINE으로 둡니다. 백그라운드에서 last_seen이 10초를 넘으면 OFFLINE과 COMMUNICATION_LOSS Alarm을 올립니다. Equipment status의 RUN/ERROR와 Connection의 ONLINE/OFFLINE은 분리했습니다. 마지막 보고가 RUN이어도 통신이 끊기면 OFFLINE입니다.

---

## 5. Recipe mismatch는 어떻게 발견하는가?

**답변 포인트:** LOT.expected_recipe_id vs event.recipe_id

30~60초:

> LOT를 seed할 때 expected_recipe를 넣습니다. 설비 이벤트에 recipe_id가 들어오면 그 둘을 비교합니다. 다르면 CRITICAL Alarm RECIPE_MISMATCH를 만들고 LOT를 HOLD로 바꿉니다. 이벤트는 이력에 남깁니다. 잘못된 보고를 숨기지 않고, 다음 공정으로 더 진행하지 못하게 막는 모델입니다.

---

## 6. 센서값 이상과 통신 장애는 어떻게 구분하는가?

**답변 포인트:** 데이터가 들어왔는가, 들어왔는데 범위 밖인가.

30~60초:

> 통신 장애는 이벤트가 없습니다. timeout 후 OFFLINE입니다. 센서 이상은 이벤트는 들어왔고, Recipe 범위와 온도/압력을 비교해 TEMPERATURE/PRESSURE Alarm을 냅니다. 같은 빨간 화면으로 묶지 않은 이유입니다. 기술지원할 때 원인 후보가 완전히 달라집니다.

---

## 7. DB에는 데이터가 있는데 화면에 없다면 어떻게 조사하는가?

**답변 포인트:** DB → backend query → API JSON → frontend 폴링.

30~60초:

> TROUBLESHOOTING CASE C입니다. 먼저 lot/process_event를 DB에서 보고, GET /api/lots 응답이 같은지 확인합니다. API가 맞는데 화면이 틀리면 Dashboard 폴링이나 잘못된 화면입니다. API가 틀리면 query 조건입니다. 이 프로젝트에서 LOT COMPLETE는 모든 Panel이 INSPECT를 끝냈을 때만이라, DB의 한 Panel 완료와 LOT 상태가 다를 수 있습니다. 그것도 먼저 구분합니다.

---

## 8. 왜 Alarm을 별도 Table로 분리했는가?

**답변 포인트:** 정상 이력과 예외의 조회 패턴이 다르다.

30~60초:

> process_event는 ‘무엇을 했는가’이고 Alarm은 ‘무엇이 잘못됐는가’입니다. 모니터링 화면은 열린 Alarm, 설비별 Alarm, 코드별 Alarm을 자주 봅니다. 이벤트 테이블에 플래그만 두면 그 조회가 지저분해지고, reject된 이벤트처럼 이력이 없는 예외를 남기기 어렵습니다. 그래서 alarm 테이블을 분리했습니다.

---

## 9. 왜 process_event와 sensor_reading을 구분했는가?

**답변 포인트:** 추적 정보와 측정값은 성격이 다르다. 실제에선 주기도 다를 수 있다.

30~60초:

> 이번 Lab에서는 JSON 한 건에 이력과 센서가 같이 옵니다. 그래도 테이블을 나눈 이유는, 공정 이력은 LOT/Panel/Recipe/Step 추적용이고 센서는 온도/압력 같은 측정값이기 때문입니다. 실제 라인에서는 센서가 이벤트보다 자주 쌓일 수 있습니다. 지금 구조면 나중에 측정값만 늘어나도 이력 모델이 덜 흔들립니다.

---

## 10. 실제 PLC가 있다면 현재 Simulator의 어느 부분을 대체하는가?

**답변 포인트:** JSON을 만들어 POST 하는 부분. 그 뒤 검증/저장/조회는 그대로 질문이다.

30~60초:

> Simulator는 설비가 아닙니다. 센서값을 만들고 HTTP POST 하는 클라이언트입니다. 실제 PLC가 있으면 그 자리, 즉 데이터 생성과 전송 구간을 설비 인터페이스가 대체합니다. FastAPI가 받은 뒤의 validation, Alarm, LOT 상태, Traceability는 같은 종류의 문제입니다. 그래서 PLC 프로토콜을 흉내 내지 않았습니다.

---

## 11. MES/CIM이 들어온다면 현재 프로젝트와 어떤 관계가 생기는가?

**답변 포인트:** 이 Lab은 MES/CIM 전체가 아니라, 그 앞에서 일어나는 수집·검증·모니터링의 축소판.

30~60초:

> 실제 MES/CIM은 작업 지시, 실적, 품질, 설비 제어 연동까지 훨씬 큽니다. 이 프로젝트는 그 전체가 아닙니다. 설비 보고를 받아 상태를 만들고 Alarm과 이력을 조회하는 작은 모델입니다. MES/CIM이 들어오면 이 서버는 수집 앞단이 되거나, MES 화면이 조회하는 데이터 소스의 아주 작은 부분에 해당합니다. 저는 그 전체 제품을 만들었다고 말하지 않습니다.

---

## 12. 현재 프로젝트가 실제 OLED/LCD 제조 시스템과 다른 점은 무엇인가?

**답변 포인트:** 공정 수, 프로토콜, 레시피 복잡도, 규모, 규정. 축소 모델임을 먼저 말한다.

30~60초:

> 실제 라인은 공정이 훨씬 많고, PLC 통신, 레시피 파라미터, 챔버 구성, 품질 시스템이 있습니다. 여기서는 LOAD, PROC, INSPECT 세 단계와 학습용 온도/압력만 있습니다. PostgreSQL과 단일 FastAPI입니다. Kafka는 넣지 않았습니다. 목적은 실제 공장을 복제하는 게 아니라, 데이터가 이동하고 문제가 생기면 어디서 확인하는지를 직접 설명하는 것입니다.

---

## 13. 왜 이 프로젝트를 만들었는가?

**답변 포인트:** 지원 직무가 시스템 개발, 테스트, 기술지원, 유지보수, 자동화 IT이기 때문.

30~60초:

> 스마트팩토리 개발직은 화면만 만드는 일이 아니라, 설비 데이터가 시스템에 들어와서 상태가 되고 장애를 추적하는 일을 포함한다고 이해했습니다. 실제 설비가 없어서 라인을 만들 수는 없었습니다. 그래서 Equipment, LOT, Recipe, Alarm, Traceability를 축소해서, 장애가 나면 Simulator부터 DB까지 어디를 볼지 연습하는 Lab을 만들었습니다.

---

## 14. C#을 왜 추가했는가?

**답변 포인트:** 산업 현장 앱 언어 연습. 산업용 제어 프로그램을 만들었다고 말하지 않는다.

30~60초:

> Python으로 서버와 Simulator MVP를 만든 뒤, 같은 PROC-01 이벤트를 C# 콘솔에서 다시 보냈습니다. class, JSON serialize, HttpClient, async/await, try/catch, timeout, 간단한 retry입니다. C#으로 설비 제어나 윈폼 모니터링을 완성했다는 뜻이 아닙니다. 다른 시스템과 HTTP로 데이터를 주고받는 기본 구조를 한 번 더 연습한 것입니다.

---

## 15. 가장 어려웠던 버그와 해결과정은 무엇인가?

**답변 포인트:** 실제 막혔던 지점 — timestamp timezone과 OFFLINE 오판, LOT COMPLETE 범위.

30~60초 초안 (구현 중 실제로 설계로 막은 버그):

> 가장 쉽게 깨지는 부분은 시간입니다. 이벤트의 timestamp가 timezone-aware이고 DB 비교값이 naive면, 모든 이벤트가 STALE로 reject되거나 반대로 timeout 계산이 틀어집니다. 그래서 입구에서 UTC naive로 정규화했습니다. 또 한 Panel이 INSPECT를 끝났다고 LOT 전체를 COMPLETE로 바꾸면 Dashboard의 PROCESSING과 DB 이력이 어긋납니다. LOT COMPLETE는 모든 Panel이 끝났을 때만으로 고쳤습니다. 기술지원 관점에서는 ‘데이터가 없는데’가 아니라 ‘완료의 단위가 LOT인지 Panel인지’를 먼저 묻는 버그였습니다.

여분의 실무형 버그 (테스트에서 재현 가능):

> 통신이 들어온 reject 이벤트도 last_seen을 갱신합니다. reject를 저장 실패와 같은 말로 보면, 잘못된 데이터가 계속 오는데도 OFFLINE으로 착각하지 않게 됩니다. OFFLINE은 ‘요청이 없다’일 때만 맞습니다.

---

## 16. OEE를 어떻게 계산했는가?

**답변 포인트:** 세 요소의 재료가 각각 어디서 오는지, 없는 재료는 없다고 말한다.

30~60초:

> 가동률은 통신 끊김·정지·정비를 구간으로 남긴 `equipment_downtime`에서, 성능은 시뮬레이터와 공유하는 공정별 이론 사이클 표에서, 품질은 유리 판정에서 옵니다. 한 장이 설비 다섯 대를 지나도 품질은 한 장으로 셉니다. 없는 재료는 계획가동시간 하나입니다. 현장 교대표가 없어서 오늘 첫 설비 보고 시각부터 지금까지를 쓰고, 어느 기준을 썼는지 응답에 같이 내보냅니다. 실적이 없으면 성능을 null로 두고 왜 못 내는지 문장으로 답합니다. 숫자를 채우려고 가짜 가동시간을 만들지 않았습니다.

---

## 17. 이상탐지에 AI를 썼는가?

**답변 포인트:** 아니라고 먼저 말한다. 한 것과 안 한 것을 나눈다.

30~60초:

> 아닙니다. 오늘 쌓인 telemetry의 평균과 표준편차를 기준으로 마지막 값이 몇 시그마인지만 냅니다. 표본이 열 개 미만이면 판단하지 않고 데이터 부족이라고 답합니다. 범위를 벗어난 값은 이미 레시피 허용폭 검증이 알람으로 잡고 있어서, 여기서는 범위 안이지만 평소와 다르게 튀는 값을 봅니다. 학습도 수명 예측도 없습니다. 그리고 분석은 설비를 직접 제어하지 않습니다. 점수를 보고 알람을 낼지, 설비를 세울지는 사람이나 MES가 명령으로 정합니다.

---

## 18. OPC UA나 Modbus는 왜 구현하지 않았는가?

**답변 포인트:** 인터페이스 계층은 열어 두고, 장비 없이 흉내 내지 않았다.

30~60초:

> 설비마다 통신 방식을 컬럼으로 갖고 있고, 수집기가 그 방식에 맞는 어댑터를 골라 부릅니다. 지금 실체가 있는 건 HTTP/JSON 시뮬레이터 어댑터 하나이고, OPC UA와 Modbus 어댑터는 호출하면 미구현 예외를 냅니다. 장비가 없는데 세션을 흉내 내면, 나중에 실제 장비를 붙일 때 그 가짜 코드를 먼저 지워야 합니다. 수집 이후 경로는 방식과 무관하게 같기 때문에, 장비가 생기면 어댑터 한 개만 채우면 됩니다.
