# DisplayFab 스마트팩토리 — 전체 흐름

OLED/LCD 가상 라인을 대상으로 **스마트팩토리 시스템**을 돌린다.

설비가 실적과 생존신호를 올리고, 호스트가 지시·조건·인터록으로 라인을 돌리고,
불량이면 멈추고 이력을 추적한다.

설비는 가상(Python/C#). 호스트는 FastAPI. 화면은 운전·생산.

```
올레드: 투입 → 세정 → 증착(1·2호기) → 봉지 → 검사
엘시디: 투입 → 배향막 → 셀 공정 → 검사
```

---

## 한 줄

> OLED/LCD 가상 라인을 대상으로 스마트팩토리 시스템을 구현했다.
> 설비가 실적과 생존신호를 올리고, 호스트가 지시·조건·인터록으로 라인을 돌리고,
> 불량이면 멈추고 이력을 추적한다.

---

## 3층

```
설비(가상)  --실적·생존신호-->  호스트
호스트      --켜기·조건------>  설비
호스트      ---------------->  생산(MES) · 운전
```

| 층 | 하는 일 | 어디 |
|---|---|---|
| 설비 | 생존신호, 실적, 센서 | Python / C# 설비 |
| 인터페이스 | 보고 수신, 명령 하달 | `/api/events`, `/api/commands` |
| 생산·운전 | 지시, 재공, 조건, 품질, 마감, 추적 | 운전 화면, 생산 화면 |

---

## 정상 한 바퀴

```
1. 지시     오늘 올레드 5장, 조건 올레드 가
2. 투입     카세트 + 유리 5장을 라인에 넣는다
3. 생존     설비가 heartbeat로 살아 있다고 알린다
4. 시작     호스트가 켜기 → 설비가 실적을 받을 수 있다
5. 공정     투입 실적 → 호스트가 다음을 세정으로 정한다
6. 이동     세정 → 증착(빈 챔버) → 봉지 → 검사
7. 검사     합격 → 그 장 완료
8. 실적     오늘 올레드 완료 1 증가
9. 마감     5장 모두 끝나면 지시 끝, 작업 완료
```

## 이상 한 바퀴

```
검사 불량 또는 조건 틀림 또는 통신 끊김
 → 보류 또는 끊김
 → 운전 화면에서 왜 멈췄는지 본다
 → 다시 보내기 / 확인 / 폐기
 → 흐름이 다시 가거나, 그 장은 끝난다
```

이 두 바퀴가 화면에서 보이면 스마트팩토리 흐름은 구현된 것이다.

---

## 한 장이 지나가는 길

```
사람
  지시 작성 → 투입 → 켜기 → (이상 시) 보류 / 확인 / 폐기
        │
        ▼
호스트
  다음 공정 지정 · 인터록 · 알람 · 수율 · 오늘 실적
        │
        ▼
설비
  생존신호  와  실적
        │
        ▼
이력
  이 유리가 어느 설비, 어느 조건, 몇 시를 지났는지
```

올레드 한 장:

```
지시 WO-OLED-01
유리 21  카세트 올레드 10
  투입      투입
  세정      세정     올레드 가
  증착 1호기 증착    올레드 가
  봉지      봉지
  검사      합격
→ 완료, 오늘 실적 +1
```

엘시디 한 장:

```
지시 WO-LCD-01
유리 31  카세트 엘시디 10
  투입      투입
  셀 공정   셀 공정   엘시디 다
  검사      합격
→ 완료
```

---

## 월요일 시연 6단

1. 지시 넣고 돌리기 → 유리가 라인을 이동한다
2. 세정 켜기 → 가동, 조건 → 올레드 가가 붙는다
3. 생존 끊기 → 그 설비 끊김, 실적 거절
4. 다시 생존 → 흐름 재개
5. 검사 불량 → 보류 → 버리기
6. 생산 화면에서 오늘 완료/불량이 맞다

---

## 화면

페이지를 늘리지 않는다. 운전 화면에 라인을 얹고, 생산 화면에 MES 네 칸을 모은다.

| 보는 것 | 어디 |
|---|---|
| 지시 만들고 투입 | 운전 위, 생산 |
| 유리가 어디로 이동 중인지 | 라인 + 대기/출고 |
| 왜 멈췄는지 | 멈춤, 알람 |
| 한 장이 지나온 길 | 지나온 길 |
| 오늘 몇 장 | 운전 마감, 생산 실적 |

---

## 이미 있는 흐름

지시, 투입, 켜기 전 실적 거절, 조건 불일치 보류, 제품 경로 거절,
증착 빈 챔버 배정, 생존 끊김, 검사 불량·폐기, 오늘 실적, 유리 이력,
지시 하나로 라인 가동.

---

## 테스트

- 지시 → 작업·유리가 생긴다
- 투입 다음이 올레드면 세정, 엘시디면 배향막
- 검사 불량 → 보류, 폐기 후 실적 거절
- 목표 대비 완료 숫자가 맞다
- 지시 하나 실행이면 올레드 5공정 전부 수락

테스트 통과한 뒤 시연 6단을 화면에서 한 번 누른다.

일정: `ROADMAP.md`
